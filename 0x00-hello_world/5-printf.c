#include <stdio.h>

/**
 * main - Defines the main funtion
 *
 * Return: 0 if runs successfully, otherwise int if error occures
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
