#include <stdio.h>

/**
 * main - Defines the main function
 *
 * Return: 0 if it runs successfully, otherwise int if error occures.
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
